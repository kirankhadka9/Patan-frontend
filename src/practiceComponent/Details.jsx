import React from 'react'

const Details = (props) => {
  return (

    <div>
        <p> My name is {props.name}</p>
        <p> My age is {props.age}</p>
        
        Details</div>
  )
}

export default Details